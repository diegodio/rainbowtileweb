import 'dart:math';
import 'listas.dart';

void mudarCor() {
  listaCores.clear();
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
  listaCores.add(bauDeCores[Random().nextInt(bauDeCores.length)]);
}
